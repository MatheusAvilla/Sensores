CREATE TABLE sensor(  
    SensorID int NOT NULL primary key AUTO_INCREMENT comment 'primary key',
    currentDate DATETIME COMMENT 'created time',
    prediction int,
) default charset utf8 comment '';
